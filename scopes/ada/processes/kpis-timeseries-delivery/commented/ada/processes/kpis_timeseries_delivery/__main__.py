# Proceso Series: lee Historian, selecciona timestamps exactos y publica una proyección compacta.
# Permite ejecutar el proceso mediante python -m sin duplicar bootstrap.

from ada.processes.kpis_timeseries_delivery.bootstrap import main

main()
