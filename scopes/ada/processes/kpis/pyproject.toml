[build-system]
requires = ["setuptools==83.0.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ada-kpis-process"
version = "0.1.0"
description = "KPI evaluation process composition for ADA."
requires-python = "==3.14.2"
classifiers = ["Private :: Do Not Upload"]
dependencies = [
    "ada-kpis-core==0.1.0",
    "ada-kpis-evaluation==0.1.0",
    "ada-kpis-persistence==0.1.0",
    "ada-kpis-sources==0.1.0",
    "atlanticus-configuration==0.1.0",
    "atlanticus-datasets==0.1.0",
    "atlanticus-datasets-parquet==0.2.0",
    "atlanticus-datasets-runtime==0.2.0",
    "atlanticus-job-runtime==0.6.0",
    "atlanticus-json==0.1.0",
    "atlanticus-kernel==0.1.0",
    "atlanticus-key-vault==0.1.0",
    "atlanticus-observability-azure==0.1.0",
    "atlanticus-state==0.1.0",
]

[dependency-groups]
dev = ["pytest==9.1.1", "ruff==0.15.22"]

[project.scripts]
ada-kpis = "ada.processes.kpis.bootstrap:main"

[tool.atlanticus.container]
command = "ada-kpis"
system-profile = "base"

[tool.uv.sources]
ada-kpis-core = { path = "../../kpis/core", editable = true }
ada-kpis-evaluation = { path = "../../kpis/evaluation", editable = true }
ada-kpis-persistence = { path = "../../kpis/persistence", editable = true }
ada-kpis-planner = { path = "../../kpis/planner", editable = true }
ada-kpis-sources = { path = "../../kpis/sources", editable = true }
ada-operational-calendar = { path = "../../operational-calendar", editable = true }
atlanticus-configuration = { path = "../../../../backend/configuration", editable = true }
atlanticus-datasets = { path = "../../../../backend/datasets", editable = true }
atlanticus-datasets-parquet = { path = "../../../../backend/datasets-parquet", editable = true }
atlanticus-datasets-runtime = { path = "../../../../backend/datasets-runtime", editable = true }
atlanticus-job-runtime = { path = "../../../../backend/runtime", editable = true }
atlanticus-json = { path = "../../../../backend/json", editable = true }
atlanticus-kernel = { path = "../../../../backend/kernel", editable = true }
atlanticus-key-vault = { path = "../../../../connectivity/key-vault", editable = true }
atlanticus-observability = { path = "../../../../backend/observability", editable = true }
atlanticus-observability-azure = { path = "../../../../backend/observability-azure", editable = true }
atlanticus-state = { path = "../../../../backend/state", editable = true }

[tool.setuptools.packages.find]
where = ["src"]
include = ["ada.processes.kpis*"]
namespaces = true

[tool.setuptools.package-data]
"ada.processes.kpis" = ["py.typed"]

[tool.pytest.ini_options]
addopts = "-q --import-mode=importlib"
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py314"
extend-exclude = ["commented"]

[tool.ruff.format]
quote-style = "single"
indent-style = "space"
line-ending = "lf"

[tool.ruff.lint]
select = ["E", "W", "F", "I", "B", "SIM"]
ignore = ["E501"]

[tool.ruff.lint.isort]
known-first-party = ["ada", "atlanticus"]
combine-as-imports = true
