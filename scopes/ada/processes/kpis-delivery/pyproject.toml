[build-system]
requires = ["setuptools==83.0.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ada-kpis-delivery-process"
version = "0.2.0"
description = "ADA KPI latest delivery process infrastructure boundary."
requires-python = "==3.14.2"
classifiers = ["Private :: Do Not Upload"]
dependencies = [
    "ada-kpis-core==0.2.0",
    "ada-kpis-delivery==0.2.0",
    "ada-kpis-persistence==0.1.1",
    "atlanticus-configuration==0.1.0",
    "atlanticus-cosmos==0.1.0",
    "atlanticus-job-runtime==0.7.1",
    "atlanticus-json==0.1.0",
    "atlanticus-kernel==0.1.0",
    "atlanticus-key-vault==0.1.0",
    "atlanticus-observability-azure==0.1.0",
    "atlanticus-state==0.2.0",
]

[dependency-groups]
dev = ["pytest==9.1.1", "ruff==0.15.22"]

[project.scripts]
ada-kpis-delivery = "ada.processes.kpis_delivery.bootstrap:main"

[tool.atlanticus.container]
command = "ada-kpis-delivery"
system-profile = "base"

[tool.uv.sources]
ada-kpis-core = { path = "../../kpis/core", editable = true }
ada-kpis-delivery = { path = "../../kpis/delivery", editable = true }
ada-kpis-persistence = { path = "../../kpis/persistence", editable = true }
atlanticus-configuration = { path = "../../../../backend/configuration", editable = true }
atlanticus-cosmos = { path = "../../../../connectivity/cosmos", editable = true }
atlanticus-json = { path = "../../../../backend/json", editable = true }
atlanticus-kernel = { path = "../../../../backend/kernel", editable = true }
atlanticus-key-vault = { path = "../../../../connectivity/key-vault", editable = true }
atlanticus-observability = { path = "../../../../backend/observability", editable = true }
atlanticus-observability-azure = { path = "../../../../backend/observability-azure", editable = true }
atlanticus-state = { path = "../../../../backend/state", editable = true }
atlanticus-job-runtime = { path = "../../../../backend/runtime", editable = true }

[tool.setuptools.packages.find]
where = ["src"]
include = ["ada.processes.kpis_delivery*"]
namespaces = true

[tool.setuptools.package-data]
"ada.processes.kpis_delivery" = ["py.typed"]

[tool.pytest.ini_options]
addopts = "-q --import-mode=importlib"
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py314"
extend-exclude = ["commented"]

[tool.ruff.format]
quote-style = "single"
indent-style = "space"
line-ending = "lf"

[tool.ruff.lint]
select = ["E", "W", "F", "I", "B", "SIM"]
ignore = ["E501"]

[tool.ruff.lint.isort]
known-first-party = ["ada", "atlanticus"]
combine-as-imports = true
