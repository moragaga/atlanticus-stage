[build-system]
requires = ["setuptools==83.0.0"]
build-backend = "setuptools.build_meta"

[project]
name = "atlanticus-datasets-runtime"
version = "0.1.0"
description = "Bidirectional Pandas and PyArrow facade for Atlanticus datasets."
readme = "README.md"
requires-python = "==3.14.2"
classifiers = [
    "Private :: Do Not Upload",
]
dependencies = [
    "atlanticus-datasets==0.1.0",
    "atlanticus-datasets-parquet==0.1.0",
    "pandas==3.0.3",
    "pyarrow==25.0.0",
]

[tool.setuptools.packages.find]
where = ["src"]
include = ["atlanticus.*"]
namespaces = true

[tool.setuptools.package-data]
"atlanticus.datasets.runtime" = ["py.typed"]
